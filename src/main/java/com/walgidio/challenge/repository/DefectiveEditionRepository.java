package com.walgidio.challenge.repository;

import com.walgidio.challenge.domain.DefectiveEdition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DefectiveEditionRepository extends JpaRepository<DefectiveEdition, Long> {
}
